html
    head
        meta(charset="utf-8")
        title upload
    body
        form(action="/upload",method="post",encType="multipart/form-data")
            Select images: 
                input(type="file",name="sampleFile",multiple)
            input(type="submit")